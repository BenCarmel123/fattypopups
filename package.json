{
  "scripts": {
    "dev": "docker-compose -f docker/docker-compose.yml --project-directory . up --build",
    "dev:down": "docker-compose -f docker/docker-compose.yml --project-directory . down"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.13"
  },
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "mathjs": "^15.0.0",
    "multer": "^2.0.2",
    "multer-s3": "^2.10.0",
    "openai": "^5.20.0",
    "twilio": "^5.13.0"
  }
}
